import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["media.istockphoto.com"] // pour accepeter des images dans le composant Image venant du domaine istock
  }
};

export default nextConfig;


